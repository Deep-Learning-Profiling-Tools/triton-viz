[mypy]
ignore_missing_imports = True

# cuda.tile kernel parameters carry RUNTIME annotations (ct.Constant[int])
# that the cuda.tile AST compiler consumes; they are not mypy types.
[mypy-evaluation.kernels.tritonracebench_cutile]
disable_error_code = valid-type
